# Yugami プロジェクト 修正タスクリスト（3Dボクセル空間への移行）

- [x] **1. 旧断層面描画の削除**
  - [x] `MapContext.js` から面のテッセレーション処理を削除し、純粋な表面ラインへと変更。
  - [x] `Visualizer.js` からMeshの描画処理を削除。
- [x] **2. 3D Voxel Grid（THREE.Points）の生成**
  - [x] `Visualizer.js` で緯度20-50、経度120-150、深度0-600kmを0.2度間隔で埋める約68万個の頂点座標を生成。
  - [x] `THREE.Points` として空間全体を定義。
- [x] **3. ボクセル向けシェーダー実装**
  - [x] **Vertex Shader**: アクティブな50個の震源との距離を全頂点で計算。総ストレス量に応じて `vStress` と `gl_PointSize` を動的変更。
  - [x] **Fragment Shader**: 柔らかい円形のパーティクル形状を適用。ストレス量に応じてベースのCyan色からPeakのPink色へ発光。
- [x] **4. 動作・負荷確認**
  - [x] 68万個のパーティクルを一度のドローコールでGPU描画するアーキテクチャへと移行完了。
