# Dependencies
node_modules/
package-lock.json
npm-debug.log
yarn-error.log
yarn.lock

# OS Generated Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor / IDE Directories
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Optional: If you do NOT want to push the downloaded earthquake data, uncomment the line below.
# (Note: Since it's < 1MB, keeping it allows others to run the app immediately without fetching data)
# data/earthquakes.json
